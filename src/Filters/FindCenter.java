package Filters;

import Interfaces.PixelFilter;
import core.DImage;

public class FindCenter implements PixelFilter {
    public static String data;

    @Override
    public DImage processImage(DImage img) {
        return null;
    }
}
